services:
  - type: web
    name: ai-battle-arena
    runtime: python310
    buildCommand: pip install -r AI\ modal/backend/requirements.txt
    startCommand: cd AI\ modal/backend && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
