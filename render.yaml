services:
  - type: web
    name: ai-battle-arena
    runtime: python310
    buildCommand: pip install gunicorn && pip install -r AI\ modal/backend/requirements.txt
    startCommand: cd AI\ modal/backend && python app.py
